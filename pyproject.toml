[project]
name = "camera-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "opencv-python>=5.0.0.93",
    "uvicorn>=0.53.0",
]
